---
paths:
  - "**/*.md"
---
Synthetic fixture rule for `path-rules` tests — not a real rule, and not loaded outside them.

When editing a Markdown file, keep headings sentence case and wrap prose at no fixed column.
