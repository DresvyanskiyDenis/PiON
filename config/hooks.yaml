# EXT-15 — declarative hook rules, loaded by extensions/hooks/index.ts.
#
# See docs/extending/hooks.md for the full field reference and the non-goal this schema is deliberately
# capped short of. Five fields per rule: event, match.tool, match.pattern, action, reason — plus
# a `run` block for the one action that shells out to a script.
#
# Merge order: this file (global, ~/.pi/agent/hooks.yaml once installed) loads first, then
# <project>/.pi/hooks.yaml when the project is trusted. A rule id should be unique across both.
#
# A rule that cannot be evaluated, whose action throws, whose script is missing, or whose
# script times out BLOCKS the tool call — it does not silently permit it. A YAML file that
# fails to parse, or doesn't match this top-level shape, degrades the layer to zero rules and
# says so loudly rather than refusing the session — see index.ts's header for why that polarity
# changed on 2026-08-11.
#
# This file is NOT linked by default. `scripts/install.sh` points ~/.pi/agent/hooks.yaml at
# config/hooks-off.yaml unless it is run with `--with-guardrails`, so nothing below is armed until
# somebody asks for it. To swap by hand, after an install:
# `ln -sf ~/pi-config/config/hooks-off.yaml ~/.pi/agent/hooks.yaml`. See docs/extending/hooks.md,
# "Turning guardrails off".
#
# What ships here, and why it is a short list. This is the rule set a public fork inherits the
# moment it opts in, so every rule in it has to be defensible on a machine whose work nobody here
# knows anything about. That rules out the three that used to sit in this file: an npm-install
# confirm (this repository's own gates run `npm ci` — a personal package-manager preference does
# not belong in a shared default), a worktree reminder matching the word "implement" in any prompt,
# and a confirm on Databricks/gcloud/aws/kubectl job submits, which is a rule about one person's
# workplace. All three are good rules for the machine they were written for; all three are still in
# the docs as worked examples, which is where a personal rule belongs. What is left is what holds
# everywhere: one refusal for a destructive, irreversible git operation, and the two `run` rules
# that arm config/constraints.json — a no-op until a project writes its own constraints.
version: 1
rules:
  - id: no-force-push-main
    event: tool_call
    match: { tool: bash, pattern: 'git\s+push\b.*(--force|-f)\b.*\b(main|master)\b' }
    action: block
    reason: "Force-pushing main/master is blocked. Push to a feature branch, or use --force-with-lease with a written reason."

  # Hard constraints. A constraint written as NEVER gets a hook; anything without one was a
  # preference. `run`, not `pattern`, because the banned patterns are per project and each carries
  # its own justification, and a refusal that cannot say WHY gets argued with. The script reads
  # config/constraints.json (global) and <project>/.pi/constraints.json, matches only the text the
  # call ADDS, and answers with that constraint's own reason. Both files absent is a no-op; a file
  # present but broken denies, because a silently disarmed constraint reads exactly like a
  # satisfied one. One rule per writing tool: `match.tool` is a single exact name, and leaving it
  # off would spawn the script on every bash and read call to answer a question it has no opinion
  # on.
  - id: constraints-edit
    event: tool_call
    match: { tool: edit }
    action: run
    run:
      command: "~/bin/pi-constraints-hook"
      timeoutMs: 5000

  - id: constraints-write
    event: tool_call
    match: { tool: write }
    action: run
    run:
      command: "~/bin/pi-constraints-hook"
      timeoutMs: 5000

# The `run` action (shells out to a script, decision on stdout or exit code 2) is documented in
# docs/extending/hooks.md with a worked example. Enable one only once the target script actually
# exists at the path you name: a `run` rule with `match: { tool: bash }` and no script in place
# would BLOCK every bash call by design (a missing script fails closed, see docs/extending/hooks.md
# "Fail-closed, on purpose"). The two `constraints-*` rules above are the first rules in this tree
# to use it, and they name a script this repository ships — config/bin/pi-constraints-hook, linked
# into ~/bin by scripts/install.sh.
